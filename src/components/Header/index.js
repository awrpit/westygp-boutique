import React from 'react';
import TopBar from './TopBar';
import TopNav from './TopNav';

export default () => {
  return (
    <header id='old-header'>
      <TopBar />
      <TopNav />
    </header>
  );
};
